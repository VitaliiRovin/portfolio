.btn-send {
    padding: 30px 120px;
    text-transform: uppercase;
    font-size: 18px;
    font-weight: bold;
    color: $white-color;
    border-radius: 40px 5px;
    background-image: linear-gradient(to right, #7c00ea, #4a00ed);

    @include tablets {
        font-size: 16px;
    }
}