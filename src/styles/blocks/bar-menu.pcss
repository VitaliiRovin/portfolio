.btn-bar__icon {
    width: 100%;
    height: 100%;
    fill: currentColor;

}

.btn-bar {
    width: 25px;
    height: 25px;
    color: $text-color;
    padding: 0;
    background-color: transparent;
    display: none;

    @include desktop {
        display: block;
    }
}